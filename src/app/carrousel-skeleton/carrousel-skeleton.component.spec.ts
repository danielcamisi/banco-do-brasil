/* import { ComponentFixture, TestBed } from '@angular/core/testing';

import { CarouselCircularDemo } from './carrousel-skeleton.component';

describe('CarrouselSkeletonComponent', () => {
  let component: CarouselCircularDemo;
  let fixture: ComponentFixture<CarouselCircularDemo>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [CarouselCircularDemo]
    })
    .compileComponents();
    
    fixture = TestBed.createComponent(CarouselCircularDemo);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */